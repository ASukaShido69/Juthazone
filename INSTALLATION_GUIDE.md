# คู่มือติดตั้งและรันโปรเจค Juthazone

## ⚠️ สำคัญ: ต้องติดตั้ง Node.js ก่อน

### 📥 ขั้นตอนที่ 1: ติดตั้ง Node.js

1. ดาวน์โหลด Node.js LTS (แนะนำ) จาก:
   - เว็บไซต์: https://nodejs.org/
   - เลือก LTS version (Long Term Support)
   - สำหรับ Windows: ดาวน์โหลดไฟล์ .msi

2. รันไฟล์ติดตั้ง:
   - เปิดไฟล์ .msi ที่ดาวน์โหลดมา
   - กด "Next" ตลอดและ "Install"
   - **สำคัญ**: ติ๊กเลือก "Automatically install necessary tools" ด้วย

3. ตรวจสอบการติดตั้ง:
   - เปิด PowerShell หรือ Command Prompt ใหม่
   - พิมพ์: `node --version`
   - ต้องเห็นหมายเลขเวอร์ชัน เช่น v20.x.x
   - พิมพ์: `npm --version`
   - ต้องเห็นหมายเลขเวอร์ชัน เช่น 10.x.x

### 📦 ขั้นตอนที่ 2: ติดตั้ง Dependencies

เปิด Terminal ใน VS Code (หรือ PowerShell) แล้วรันคำสั่ง:

```bash
npm install
```

รอให้ติดตั้งเสร็จ จะใช้เวลาประมาณ 1-2 นาที

### 🚀 ขั้นตอนที่ 3: รันโปรเจค

รันคำสั่ง:

```bash
npm run dev
```

เมื่อรันสำเร็จ จะเห็นข้อความคล้ายๆ:
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

### 🌐 ขั้นตอนที่ 4: เปิดในเบราว์เซอร์

1. เปิดเบราว์เซอร์ (Chrome, Edge, Firefox)
2. ไปที่ URL: `http://localhost:5173/admin`
3. หน้าแอดมินจะเปิดขึ้นมา

> **หมายเหตุสำหรับฟีเจอร์ขายสินค้า** – หากต้องการใช้งานระบบขายสินค้าในหน้าแอดมิน
> ให้สร้างตารางฐานข้อมูล `historyforproduct` โดยรันสคริปต์
> `sql/CREATE_HISTORYFORPRODUCT_TABLE.sql` ในฐานข้อมูล Supabase/ PostgreSQL  ของคุณ
> ก่อนเริ่มใช้งาน (หรือสร้างตารางด้วยโครงสร้างเดียวกันเอง)

### 📱 การเข้าถึงหน้าลูกค้า

- **หน้าแอดมิน**: http://localhost:5173/admin
- **หน้าลูกค้า**: http://localhost:5173/customer

หรือสแกน QR Code ที่แสดงในหน้าแอดมิน

### 🛑 หยุดการทำงาน

กด `Ctrl + C` ใน Terminal เพื่อหยุดเซิร์ฟเวอร์

---

## 🔧 แก้ปัญหา

### ถ้า npm ไม่ทำงาน
- ปิด Terminal แล้วเปิดใหม่
- ตรวจสอบว่าติดตั้ง Node.js เสร็จแล้ว
- รัน `node --version` เพื่อเช็ค

### ถ้า port 5173 ถูกใช้งานอยู่
- Vite จะเปลี่ยนไปใช้ port อื่นอัตโนมัติ
- ดู URL ที่แสดงใน Terminal

### ถ้าไม่เห็น QR Code
- ตรวจสอบว่าติดตั้ง dependencies ครบ
- ลองรัน `npm install` อีกครั้ง

---

## 📝 คำสั่งที่ใช้บ่อย

```bash
# ติดตั้ง dependencies
npm install

# รันโปรเจคแบบ development
npm run dev

# Build สำหรับ production
npm run build

# Preview build
npm run preview
```

---

## 🎯 สิ่งที่ควรรู้

1. **State ไม่ถูกเก็บ**: ข้อมูลจะหายเมื่อปิดหน้าเว็บ (ตาม requirement ที่ไม่ใช้ localStorage)
2. **Real-time Update**: เวลาจะอัพเดททุกวินาที
3. **Responsive**: ใช้งานได้บนมือถือ
4. **Multiple Devices**: เปิดหน้าลูกค้าได้หลายอุปกรณ์พร้อมกัน

---

## ✅ Checklist การตั้งค่า

- [ ] ติดตั้ง Node.js แล้ว (ตรวจสอบด้วย `node --version`)
- [ ] รัน `npm install` เสร็จแล้ว
- [ ] รัน `npm run dev` สำเร็จ
- [ ] เปิดหน้าเว็บได้แล้ว
- [ ] ทดสอบเพิ่มลูกค้าได้
- [ ] เห็น QR Code
- [ ] หน้าลูกค้าแสดงข้อมูลได้

---

**หมายเหตุ**: หากมีปัญหาหรือคำถาม สามารถดูข้อมูลเพิ่มเติมได้ที่ README.md
