# 🚀 คู่มือ Deploy Juthazone ไปยัง Vercel

## 📋 ข้อกำหนดเบื้องต้น

1. ✅ ติดตั้ง Node.js แล้ว (ดูใน INSTALLATION_GUIDE.md)
2. ✅ บัญชี GitHub (สมัครฟรีที่ https://github.com)
3. ✅ บัญชี Vercel (สมัครฟรีที่ https://vercel.com)

---

## 🔧 ขั้นตอนที่ 1: ทดสอบ Build Production

ก่อน deploy ควรทดสอบ build ในเครื่องก่อน:

```bash
# ติดตั้ง dependencies (ถ้ายังไม่ได้ทำ)
npm install

# ทดสอบ build
npm run build

# ทดสอบ preview
npm run preview
```

ถ้า build สำเร็จจะเห็นโฟลเดอร์ `dist` ถูกสร้างขึ้น

---

## 🌐 ขั้นตอนที่ 2: อัพโหลดโค้ดไปยัง GitHub

### วิธีที่ 1: ใช้ VS Code (แนะนำ)

1. เปิด Source Control ใน VS Code (Ctrl+Shift+G)
2. คลิก "Initialize Repository"
3. เขียน commit message เช่น "Initial commit - Juthazone"
4. คลิก "Commit"
5. คลิก "Publish Branch"
6. เลือก "Publish to GitHub public repository"
7. ตั้งชื่อ repository เช่น "juthazone"

### วิธีที่ 2: ใช้ Git Command Line

```bash
# Initialize git repository
git init

# Add all files
git add .

# Commit
git commit -m "Initial commit - Juthazone"

# Create new repository on GitHub then:
git remote add origin https://github.com/YOUR_USERNAME/juthazone.git
git branch -M main
git push -u origin main
```

---

## ⚡ ขั้นตอนที่ 3: Deploy ไปยัง Vercel

### วิธีที่ 1: Deploy ผ่าน Vercel Dashboard (แนะนำสำหรับมือใหม่)

1. เข้าไปที่ https://vercel.com
2. คลิก "Sign Up" และเลือก "Continue with GitHub"
3. อนุญาตให้ Vercel เข้าถึง GitHub
4. คลิก "Add New..." → "Project"
5. เลือก repository "juthazone" ที่เพิ่งสร้าง
6. กด "Import"
7. ตั้งค่าโปรเจค:
   - **Framework Preset**: Vite
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
8. คลิก "Deploy"
9. รอประมาณ 1-2 นาที จนกระทั่งเสร็จ
10. คลิก "Visit" เพื่อดูเว็บไซต์

### วิธีที่ 2: Deploy ผ่าน Vercel CLI

```bash
# ติดตั้ง Vercel CLI
npm install -g vercel

# Login
vercel login

# Deploy
vercel

# Deploy to production
vercel --prod
```

---

## 🎯 หลัง Deploy แล้ว

### URL ที่ได้

Vercel จะสร้าง URL ให้อัตโนมัติ เช่น:
- `https://juthazone.vercel.app`
- `https://juthazone-username.vercel.app`

### เส้นทางสำคัญ

- **หน้าแอดมิน**: `https://your-app.vercel.app/admin`
- **หน้าลูกค้า**: `https://your-app.vercel.app/customer`

### QR Code

QR Code ในหน้าแอดมินจะอัพเดท URL เป็น production URL อัตโนมัติ!

---

## 🔄 การอัพเดทเว็บไซต์

เมื่อแก้ไขโค้ด ให้:

```bash
# Commit changes
git add .
git commit -m "Update: description of changes"

# Push to GitHub
git push

# Vercel จะ deploy อัตโนมัติ!
```

---

## ⚙️ การตั้งค่าเพิ่มเติม

### Custom Domain (ถ้าต้องการ)

1. ไปที่ Project Settings ใน Vercel
2. คลิก "Domains"
3. เพิ่ม domain ของคุณ เช่น `juthazone.com`
4. ทำตาม DNS instructions

### Environment Variables (ถ้ามี)

1. ไปที่ Project Settings → Environment Variables
2. เพิ่ม variables ที่ต้องการ
3. Redeploy โปรเจค

---

## 🐛 แก้ปัญหา

### Build ไม่ผ่าน

```bash
# ลองลบ node_modules แล้วติดตั้งใหม่
rm -rf node_modules
npm install
npm run build
```

### Routing ไม่ทำงาน

- ตรวจสอบว่ามีไฟล์ `vercel.json` แล้ว ✅
- ไฟล์นี้จะทำให้ React Router ทำงานได้บน Vercel

### Page ไม่แสดงผล

- ตรวจสอบ Console ใน DevTools (F12)
- ดู Build Logs ใน Vercel Dashboard

---

## 📊 Dashboard Vercel

ใน Vercel Dashboard คุณสามารถดูได้:

- 📈 Analytics (จำนวนผู้เข้าชม)
- 🚀 Deployment History
- 📝 Build Logs
- ⚡ Performance Metrics

---

## 💡 Tips

1. **Preview Deployments**: ทุก branch ที่ push จะได้ preview URL
2. **Auto Deploy**: Push to main = Auto deploy to production
3. **Instant Rollback**: สามารถย้อนกลับไปเวอร์ชันเก่าได้ทันที
4. **Free Plan**: เพียงพอสำหรับโปรเจคขนาดเล็ก-กลาง

---

## ✅ Checklist

- [ ] Node.js ติดตั้งแล้ว
- [ ] โปรเจค build ผ่าน (`npm run build`)
- [ ] โค้ดอัพโหลดไป GitHub แล้ว
- [ ] เชื่อมต่อ GitHub กับ Vercel แล้ว
- [ ] Deploy สำเร็จ
- [ ] ทดสอบหน้า /admin และ /customer
- [ ] QR Code แสดงผลและสแกนได้

---

## 🎉 สำเร็จ!

เว็บไซต์ Juthazone ของคุณพร้อมใช้งานออนไลน์แล้ว! 🚀

- ⚡ โหลดเร็ว (Powered by Vercel Edge Network)
- 🌍 เข้าถึงได้ทั่วโลก
- 🔒 HTTPS อัตโนมัติ
- 🆓 ฟรี!

---

**หมายเหตุ**: URL ที่ได้จะใช้ได้ตลอดไป และ Vercel จะอัพเดทอัตโนมัติทุกครั้งที่คุณ push โค้ดใหม่!
