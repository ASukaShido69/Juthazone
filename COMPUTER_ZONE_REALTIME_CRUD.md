# Computer Zone Realtime CRUD - คู่มือการใช้งาน

## ✨ ฟีเจอร์ใหม่

### 1. **Realtime Subscription**
ระบบจะอัปเดตข้อมูลอัตโนมัติทันทีเมื่อมีการเปลี่ยนแปลงในตาราง `computer_zone_history`:
- ✅ เพิ่มรายการใหม่ (INSERT)
- ✅ แก้ไขรายการ (UPDATE)
- ✅ ลบรายการ (DELETE)

**ทำงานโดย:**
```javascript
supabase
  .channel('computer_zone_changes')
  .on('postgres_changes', { event: '*', table: 'computer_zone_history' }, ...)
  .subscribe()
```

### 2. **CRUD Operations**

#### 📝 Create (เพิ่ม)
- ใช้ `ComputerZoneEntry` component ที่หน้า Admin Dashboard
- บันทึกลง Supabase → Realtime จะแจ้งเตือนหน้า Daily Summary ทันที

#### 📖 Read (อ่าน)
- `DailySummaryView` แสดงรายการทั้งหมด
- ดึงข้อมูลด้วย `loadDailySummary()` หรือ `loadRangeSummary()`
- อัปเดตอัตโนมัติเมื่อมีการเปลี่ยนแปลง

#### ✏️ Update (แก้ไข) - **ใหม่!**
- คลิกปุ่ม "✏️ แก้ไข" ที่แถวที่ต้องการแก้
- แก้ไขข้อมูลในฟิลด์ inline (ชื่อ, ชั่วโมง, โอน, เงินสด, เวลา, หมายเหตุ)
- คลิก "💾 บันทึก" เพื่อ save หรือ "✖️ ยกเลิก" เพื่อยกเลิก
- ระบบจะอัปเดต Supabase → Realtime แจ้งเตือนทันที

#### 🗑️ Delete (ลบ)
- คลิกปุ่ม "🗑️ ลบ" ที่แถวที่ต้องการลบ
- ยืนยันการลบ
- ระบบจะลบจาก Supabase → Realtime แจ้งเตือนทันที

---

## 🎯 ตัวอย่างการใช้งาน

### สถานการณ์: มี 2 หน้าต่างเปิดพร้อมกัน

**หน้าต่างที่ 1: Admin Dashboard (เพิ่มข้อมูล)**
```
พนักงาน A เพิ่มรายการ: ลูกค้า "สมชาย", 2 ชม., ฿100 โอน
→ กด "เพิ่มรายการ"
```

**หน้าต่างที่ 2: Daily Summary (ดูรายงาน)**
```
→ รายการ "สมชาย" ปรากฏทันที โดยไม่ต้อง refresh! ✅
```

---

### สถานการณ์: แก้ไขข้อมูลผิด

**หน้า Daily Summary:**
```
1. เห็นรายการ "สมชาย - 2 ชม. - ฿100"
2. คลิก "✏️ แก้ไข"
3. แก้ไขชั่วโมงเป็น 3 ชม., ยอดเป็น ฿150
4. คลิก "💾 บันทึก"
→ รายการอัปเดตทันที ✅
```

**หน้าต่างอื่นที่เปิด Daily Summary:**
```
→ เห็นการเปลี่ยนแปลงทันทีโดยไม่ต้อง refresh! ✅
```

---

## 🔧 Technical Details

### State Management
```javascript
const [editingEntry, setEditingEntry] = useState(null)
const [editFormData, setEditFormData] = useState({
  customerName: '',
  hours: '',
  transferAmount: '',
  cashAmount: '',
  startTime: '',
  description: ''
})
```

### Edit Functions
```javascript
startEditEntry(entry)    // เริ่มแก้ไข
cancelEdit()             // ยกเลิกการแก้ไข
updateComputerEntry(id)  // บันทึกการแก้ไข
```

### Realtime Setup
```javascript
useEffect(() => {
  const channel = supabase
    .channel('computer_zone_changes')
    .on('postgres_changes', { event: '*', table: 'computer_zone_history' }, 
      (payload) => {
        // Reload data automatically
        loadDailySummary() // or loadRangeSummary()
      }
    )
    .subscribe()

  return () => supabase.removeChannel(channel)
}, [viewMode, summaryData.date, ...])
```

---

## ⚠️ สิ่งที่ต้องระวัง

1. **การตรวจจับกะ**: ระบบจะตรวจจับกะอัตโนมัติจาก `start_time` ตอนแก้ไข
2. **ชื่อลูกค้า**: ถ้าไม่กรอกจะใช้ "ไม่ระบุชื่อ" แทน
3. **Validation**: ต้องกรอกชั่วโมง และต้องกรอกยอดเงิน (โอนหรือเงินสด) อย่างน้อย 1 อย่าง

---

## 🎉 ประโยชน์

✅ **ทำงานร่วมกันได้**: หลายคนดูหน้า Daily Summary พร้อมกัน เห็นข้อมูลเหมือนกัน
✅ **แก้ไขง่าย**: แก้ไขผิดพลาดได้ทันที ไม่ต้องลบแล้วเพิ่มใหม่
✅ **ไม่ต้อง Refresh**: ข้อมูลอัปเดตอัตโนมัติ
✅ **สะดวกรวดเร็ว**: แก้ไข inline ในตารางเดียวกัน

---

## 📊 UI ที่เปลี่ยนไป

### ก่อน (Old)
```
| ลำดับ | ชื่อ | ชั่วโมง | ... | [ลบ] |
```

### หลัง (New)
```
| ลำดับ | ชื่อ | ชั่วโมง | ... | [✏️ แก้ไข] [🗑️ ลบ] |
```

**ตอนแก้ไข (Edit Mode):**
```
| ลำดับ | [input ชื่อ] | [input ชั่วโมง] | ... | [💾 บันทึก] [✖️ ยกเลิก] |
```

---

ระบบพร้อมใช้งาน Realtime CRUD เต็มรูปแบบแล้ว! 🚀
